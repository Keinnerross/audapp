<style>
    /* Página sin márgenes SOLO para la portada */
    @page:first {
      margin: 0 !important;
    }
  
    /* Páginas siguientes con márgenes amplios */
    @page {
      size: A4;
      margin: 1cm 0cm 0cm 0cm;
    }
  
    body {
      font-family: Arial, sans-serif;
      padding: 0;
      margin: 0;
    }
  
    /* ========== PORTADA ========== */
    .pagina-portada {
      width: 100%;
      height: 100vh;
      margin: 0;
      padding: 0;
      position: relative;
      page-break-after: always;
    }
  
    /* ========== CONTENIDO CON MÁRGENES ========== */
    .wrapper {
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
    }
  
    .div-margenes {
      padding: 4rem;
      display: flex;
      flex-direction: column;
      align-items: center;
      width: 100%;
    }
  
    h1, h2, h3 {
      color: #005077;
      margin-bottom: 10px;
    }
  
    table {
      width: 100%;
      border-collapse: collapse;
      margin: 10px 0 20px 0;
    }
  
    th, td {
      border: 1px solid #000;
      padding: 8px;
      font-size: 13px;
      text-align: left;
      vertical-align: top;
    }
  
    th {
      background-color: #d4c09b;
    }
  
    .section-header {
      background-color: #c8d9ef;
      padding: 8px 12px;
      font-size: 14px;
      width: 100%;
      margin-top: 50px;
      page-break-inside: avoid;
      margin-bottom: 30px;
    }
  
    ul {
      margin: 0;
      padding-left: 20px;
    }
  
    /* Prevenir cortes incómodos */
    .section-header, table, tr, td, th {
      page-break-inside: avoid !important;
      break-inside: avoid !important;
    }
  
    .bloque-seccion-completa {
      page-break-inside: avoid;
      break-inside: avoid;
      width: 100%;
      margin-bottom: 40px;
    }
  
    .requerimiento-empty {
      width: 100%;
      text-align: center;
      font-style: italic;
      color: #555;
      padding: 25px 0 50px 0;
    }
  
    .bloque-requerimiento {
      display: flex;
      flex-direction: column;
      gap: 20px;
      width: 100%;
      page-break-inside: avoid;
    }
  
    .requerimiento-table th {
      background-color: #C6B491;
      text-align: center;
    }
  </style>
  